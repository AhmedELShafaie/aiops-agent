apiVersion: v1
kind: Service
metadata:
  name: ingest-gateway
  namespace: aiops
spec:
  selector:
    app: ingest-gateway
  ports:
    - name: http
      port: 80
      targetPort: 8001
---
apiVersion: v1
kind: Service
metadata:
  name: approval-orchestrator
  namespace: aiops
spec:
  selector:
    app: approval-orchestrator
  ports:
    - name: http
      port: 80
      targetPort: 8004
